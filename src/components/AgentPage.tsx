import React from 'react'
//TODO the Agentpage should contain a gpt section where user can interact with the agent
//TODO below we need to add the select agent section where user can select the agent and then interact with it
//TODO at left we need to have a text area where the response generated can be shown
// TODO Emails must have category like single email or a bulk email recepient 
function AgentPage() {
  return (
    <div>
      
    </div>
  )
}

export default AgentPage
